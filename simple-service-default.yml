eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: simple-service
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.value}

message: Hello from config repo def
name: Bob def